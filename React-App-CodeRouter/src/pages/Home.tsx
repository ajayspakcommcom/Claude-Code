// FILE: src/pages/Home.tsx
// ROUTE: / — defined in router.tsx as indexRoute

import { Link } from "@tanstack/react-router";

export function HomePage() {
  return (
    <div>
      <h2>🏠 Home — Code-Based Routing</h2>
      <p>
        This project uses <strong>TanStack Router code-based routing</strong>.
        Routes are defined manually in <code>src/router.tsx</code> using
        <code>createRoute()</code> and assembled with <code>addChildren()</code>.
        No file conventions. No Vite plugin. No code generation.
      </p>

      <h3>Code-based vs File-based — side by side:</h3>
      <table style={{ borderCollapse: "collapse", fontSize: "13px", width: "100%" }}>
        <thead>
          <tr style={{ background: "#f5f5f5" }}>
            <th style={{ padding: "8px 12px", textAlign: "left" }}>Task</th>
            <th style={{ padding: "8px 12px", textAlign: "left" }}>File-based (FileRouter)</th>
            <th style={{ padding: "8px 12px", textAlign: "left" }}>Code-based (this project)</th>
          </tr>
        </thead>
        <tbody>
          {[
            ["Add a route",        "Create src/routes/about.tsx",             "createRoute({ path: '/about' }) in router.tsx"],
            ["Dynamic param",      "Name file $userId.tsx",                   "path: '$userId' in createRoute()"],
            ["Nested routes",      "Put files in a folder",                   "route.addChildren([childRoute])"],
            ["Root layout",        "Create __root.tsx",                       "createRootRoute({ component: Layout })"],
            ["Route tree",         "Auto-generated by Vite plugin",           "Assembled manually with addChildren()"],
            ["Type safety",        "Inferred from file/folder names",         "Inferred from getParentRoute chain"],
            ["Loader, guards etc", "Same API — createFileRoute options",      "Same API — createRoute options"],
          ].map(([task, fileBased, codeBased]) => (
            <tr key={task} style={{ borderBottom: "1px solid #eee" }}>
              <td style={{ padding: "8px 12px", fontWeight: "bold" }}>{task}</td>
              <td style={{ padding: "8px 12px", fontFamily: "monospace", color: "#4a90e2" }}>{fileBased}</td>
              <td style={{ padding: "8px 12px", fontFamily: "monospace", color: "#e67e22" }}>{codeBased}</td>
            </tr>
          ))}
        </tbody>
      </table>

      <div style={{ marginTop: "16px", display: "flex", gap: "8px" }}>
        <Link to="/users"><button>View Users →</button></Link>
        <Link to="/products"><button>View Products →</button></Link>
      </div>
    </div>
  );
}
